# Day 02 — Arrays & Loops

## 🧠 Learnings

- Iterating arrays with `forEach()`
- Transforming arrays with `map()`
- Filtering arrays with `filter()`
- Aggregating arrays with `reduce()`
- Loops recap: for, while, do-while
- Mini exercises with numbers, strings, and objects

## 📂 Exercises Included

1. `forEach()`  
   - Print index + element  
   - Double each number  
   - Sum all numbers  
   - Filter strings by length  
   - Capitalize all names  

2. `map()`  
   - Celsius → Fahrenheit  
   - Name lengths  
   - Append stars to names  
   - Extract 'name' property  

3. `filter()`  
   - Odd numbers  
   - Names longer than 4 letters  
   - Products priced above 10000  
   - Fruits containing "a"  

4. `reduce()`  
   - Sum numbers  
   - Multiply numbers  
   - Count total letters  
   - Total price of products  
   - Flatten 2D array
